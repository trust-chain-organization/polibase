{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/Users/okodoon/project/polibase"
      ],
      "description": "Access to the polibase project files"
    },
    "postgres": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://sagebase_user:sagebase_password@localhost:5432/sagebase_db"
      ],
      "description": "Direct access to the Polibase PostgreSQL database"
    }
  }
}
